<!-- Page Content -->
<div id="page-wrapper">
    <div class="container-fluid">
        <div class="row">
            <div class="col-lg-12">
                <h3>Lista de Documentos Protocolados</h3>
                <i class="fa fa-list"></i> <?= $this->view->title; ?><br>
                <a href="<?=$this->view->controller;?>" class="btn btn-warning pull-right"><i class="fa fa-arrow-left"></i> Voltar</a>
                <table class="table">
                    <thead>
                        <tr>
                            <th>Número</th>
                            <th>Tipo</th>
                            <th>Valor (R$)</th>
                            <th>OM</th>
                            <th></th>
                        </tr>
                    </thead>
                    <tbody>
                        <?php foreach ($this->view->result as $value) : ?>
                            <tr>
                                <td><?= $value['document_number']; ?></td>
                                <td><?= $value['modality']; ?></td>
                                <td><?= 'R$ ' . number_format($value['document_value'], 2, ',', '.'); ?></td>
                                <td><?= $value['naval_indicative']; ?></td>
                                <td>
                                    <a href="<?= $this->view->controller; ?>detalhar/idlista/<?= $value['id']; ?>" class="btn btn-primary">
                                        <i class="fa fa-eye"></i> Detalhar
                                    </a>
                                    <a href="<?= $this->view->controller; ?>historico/id/<?= $value['id']; ?>" class="btn btn-success">
                                        <i class="fa fa-eye"></i> Histórico do Documento
                                    </a>
                                    <a href="<?= $this->view->controller; ?>status/id/<?= $value['id']; ?>" class="btn btn-info">
                                        <i class="fa fa-plus-square"></i> Adicionar Status
                                    </a>
                                </td>
                            </tr>
                        <?php endforeach; ?>
                    </tbody>
                </table>

                <nav>
                    <ul class="pagination">
                        <li>
                            <a href="<?= $this->view->controller; ?>protocolado/om/<?= $this->view->result[0]['om']; ?>/pagina/<?= $this->view->btn['previus']; ?>" aria-label="Previous">
                                <span aria-hidden="true">&laquo;</span>
                            </a>
                        </li>
                        <?php foreach ($this->view->btn['link'] as $value) : ?>
                            <li><a href="<?= $this->view->controller; ?>protocolado/om/<?= $this->view->result[0]['om']; ?>/pagina/<?= $value; ?>"><?= $value; ?></a></li>
                        <?php endforeach; ?>
                        <li>
                            <a href="<?= $this->view->controller; ?>protocolado/om/<?= $this->view->result[0]['om']; ?>/pagina/<?= $this->view->btn['next']; ?>" aria-label="Next">
                                <span aria-hidden="true">&raquo;</span>
                            </a>
                        </li>
                    </ul>
                </nav>

            </div>
            <!-- /.col-lg-12 -->
        </div>
        <!-- /.row -->
    </div>
    <!-- /.container-fluid -->
</div>
<!-- /#page-wrapper -->